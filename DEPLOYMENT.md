# 微信云托管部署清单（当前推荐）

当前小程序已经配置为调用你的微信云托管环境：

- 环境 ID：`prod-d7gf8lw3n60ac72fa`
- 服务名：`express-k468`

小程序经 `wx.cloud.callContainer` 调用这个服务，不需要在小程序后台配置 request 域名，也不需要自行购买、备案域名。首次调用前必须在小程序 `app.js` 初始化云环境；该改动已完成。

## 下一步：替换示例服务

1. 在云托管控制台点击“完成”，进入 **服务详情 → express-k468**。
2. 选择“新建版本 / 部署”，部署方式选“本地上传代码”。
3. 将本项目根目录打包上传；压缩包顶层必须直接包含 `Dockerfile`、`package.json` 和 `server/`，不能再多套一层目录。
4. 构建完成后，把新版本流量设为 100%，再访问服务的 `/health` 检查是否返回 `{"ok":true}`。
5. 不需要把页面显示的公网域名填入小程序，也不要在小程序后台添加它。服务只供本小程序通过云托管内网访问即可。

## 数据持久化

服务端已接入 MySQL：会将长期积分、房间、补分流水、结算、以及未结束牌局的完整状态写入 MySQL。云托管“服务设置”里配置以下环境变量即可启用：

- `MYSQL_ADDRESS`：例如 `10.x.x.x:3306`
- `MYSQL_USERNAME`
- `MYSQL_PASSWORD`
- 可选 `MYSQL_DATABASE`，默认 `zhuocheng`

容器本地 JSON 只作为未配置 MySQL 时的开发回退，不能用于正式熟人局。

## 仍需用户完成的微信环境关联

小程序 AppID 必须关联到当前 CloudBase 环境，才能让 `wx.cloud.callContainer` 调用云托管；这不是配置公网域名可以替代的步骤。关联成功后，重新编译小程序并验证“创建房间”。如果失败，再到云托管运行日志查看服务端错误。

# 自建服务器部署清单（备选）

## 运行服务

1. 复制 `.env.example` 为服务器上的 `.env`，填入真实密钥；不要提交该文件。
2. 在服务器执行 `docker compose up -d --build`。
3. 用反向代理将一个 HTTPS 域名转发给 `127.0.0.1:8787`，并同时支持 WebSocket Upgrade。
4. 访问 `https://你的域名/health`，应返回 `{ "ok": true }`。

## 微信小程序后台

1. 创建并完成认证的小程序，获得 AppID。
2. 将 AppID 写入 `project.config.json`；AppSecret 仅写入服务器 `.env`。
3. 在“开发管理 → 开发设置”登记 HTTPS request 域名和 WSS socket 域名。
4. 在微信开发者工具中启用正式 AppID，完成真机登录、创建/加入房间、四人同步、结算、历史记录测试。
5. 按实际收集的数据更新隐私政策；语音功能必须仅在用户主动开启麦克风后请求权限。

## 语音服务

当前代码只签发权限边界与短期凭据接口。需要选择并配置一个支持小程序的实时音视频服务，再将服务端签发逻辑接入 `server/voice.js`。厂商密钥只能存在服务器环境变量中。

## 生产注意事项

- 正式熟人局必须确认 MySQL 已连通，并为数据库开启备份。
- 反向代理必须启用 HTTPS/WSS；微信真机不允许把 `127.0.0.1` 当服务域名。
- 上线前需要加速率限制、会话鉴权、日志脱敏和数据库备份。
